import React from 'react';
import '../styles/Home.css';

const Loader = () => (
  <div className="loader-overlay">
    <div className="loader loader-large" aria-label="Loading"></div>
  </div>
);

export default Loader;
